/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  output: 'standalone', // Necessary for optimized production builds in some environments
};

export default nextConfig;
